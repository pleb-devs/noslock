export interface BuildConnectionStringOptions {
    pubkey: string;
    relays?: string[];
    secret?: string;
}
/**
 * Build a bunker connection string from parameters.
 */
export declare function buildConnectionString(options: BuildConnectionStringOptions): string;
import type { NIP46ConnectionInfo } from "../types";
/**
 * Parse a bunker or nostrconnect connection string with enhanced security validation.
 */
export declare function parseConnectionString(str: string): NIP46ConnectionInfo;
